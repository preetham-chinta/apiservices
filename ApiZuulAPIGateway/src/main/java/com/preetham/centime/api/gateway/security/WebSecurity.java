package com.preetham.centime.api.gateway.security;

/*
 * //@Configuration public class WebSecurity extends
 * WebSecurityConfigurerAdapter {
 * 
 * @Override protected void configure(HttpSecurity http) throws Exception {
 * http.csrf().disable(); http.headers().frameOptions().disable(); //
 * http.authorizeRequests() //add for h2 console also //
 * .antMatchers(HttpMethod.POST,"for regis").permitAll() //
 * .antMatchers(HttpMethod.POST,"for login").permitAll()
 * http.sessionManagement().sessionCreationPolicy(SessionCreationPolicy.
 * STATELESS); super.configure(http); } }
 */
